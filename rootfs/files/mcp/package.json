{
  "name": "cracked-browser",
  "version": "1.0.0",
  "private": true,
  "description": "The only Node package the guest still needs: the MCP server agentd drives Chrome through.",
  "dependencies": {
    "chrome-devtools-mcp": "1.8.0"
  },
  "engines": {
    "node": ">=23"
  }
}
